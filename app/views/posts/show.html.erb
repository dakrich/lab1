<h1> Details </h1>
<h2><%= @post.title %></h2>

<div><%= @post.author%></div>
<div><%= @post.genre%></div>
<div><%= @post.price%></div>
<div><%= @post.publisheddate%></div>

<%= link_to "Update Book", edit_post_path(@post)%>
<%= link_to "Delete Book", post_path(@post), method: :delete, data: {confirm: "Are you sure?"}%>